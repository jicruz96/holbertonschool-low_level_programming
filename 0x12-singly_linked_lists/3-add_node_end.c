#include "lists.h"
#include <stdlib.h>

/**
 * add_node_end - adds a new node at the end of a linked list
 *
 * @list_t: list where we'll add a node
 *
 * Return: address of new element, or NULL if malloc fails
 */

list_t *add_node_end(list_t **head, const char *str)
{

}
