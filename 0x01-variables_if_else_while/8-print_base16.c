#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c, b;

	c = '48';
	b = '97'
	while (c <= '57')
	{
		putchar (c);
		c++;
	}
	while (b <= 102)
	{
		putchar (b);
		b++;
	}
	putchar ('\n');
	return (0);
}
